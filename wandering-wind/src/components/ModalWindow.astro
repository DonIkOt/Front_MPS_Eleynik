  <div class="modal js-modal">
        <div class="modal__bg"></div>
        <div class="modal__wrap">
            <h3>Добавить новый проект</h3>
            <input type="text" id="project-title" placeholder="Название проекта" class="modal__input">
            <textarea id="project-description" placeholder="Описание проекта" class="modal__input"></textarea>
            <div class="modal__buttons">
                <button id="modal-ok-button" class="contact-button">Ок</button>
                <button type="button" class="modal__close">Отмена</button>
            </div>
        </div>
    </div>

 <style>
 .modal {
    position: fixed;
    width: 100vw;
    height: 100vh;
    top: 0;
    left: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 10;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.4s ease-in-out;
}

.modal.open {
    pointer-events: auto;
    opacity: 1;
}

.modal__bg {
    position: absolute;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
    top: 0;
    left: 0;
}

.modal__wrap {
    position: relative;
    max-width: 90%;
    max-height: 90vh;
    width: 416px;
    padding: 26px 42px 20px;
    background-color: white;
    border-radius: 16px;
    z-index: 1;
}

.modal__close {
    position: absolute;
    top: 20px;
    right: 14px;
    cursor: pointer;
    border: none;
    background: none;
    font-size: 16px;
}

.modal__input {
    width: 100%;
    padding: 10px;
    margin-bottom: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
}

.modal__buttons {
    display: flex;
    justify-content: space-between;
}
</style>   